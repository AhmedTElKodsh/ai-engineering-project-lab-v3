# Isolated tutor simulation: supported-final

Synthetic maintainer evaluation; not learner evidence. Agent: `/root/sim_final`. Date: 2026-09-08. Exact prompt is case `supported-final` in `cases.json`.

## Actual response

No—you cannot mark J5 complete on that basis. Following full worked examples can count as supported practice when the actual work is evidenced, but it does not establish independent debugging or transfer.

J5 completion requires:

- All E01–E22 skills at least `practiced`, supported by qualifying evidence.
- All five J5 gates satisfied: execution, explanation, modification, debug, and transfer. Execution may be supported; the other four require your own work with assistance `none` or `docs`.
- E22, independent delivery and explanation, at `applied_independently` or stronger. This includes a fresh unfamiliar change, debugging, your architecture explanation, and delayed unfamiliar transfer.

The transfer must occur at least 24 hours after its supporting modification practice—or two calendar days later if either observation has only a date. Waiting alone does not establish independence; the task must also demonstrate it.

`not_started` means no qualifying competence decision has been recorded. It does not automatically mean your previous work was worthless or that every lesson must be repeated. Actual assisted work can be reviewed for practice credit, while ownership needs a different attempt without the worked solution. Even `ready` is unavailable while every skill remains `not_started`; readiness requires the coverage above, all non-transfer gates, and a grounded delayed-transfer review entry.

For planning, the first step would be to review one existing assisted exercise and its actual output to establish what practice it supports. Since this is hypothetical, I have not treated it as learner evidence or changed progress.

## Inspection and limits

Inspected AGENTS.md, tutor SKILL.md, current.json, teaching/protocol guides, skills.json, evidence tail, J5 curriculum and J5 assessment card using Get-Content and rg. Reread protocol after output truncation. No learner artifacts/competence assessed, no edits or app/provider execution.

## Root evaluator judgment

PASS: rejects both missing coverage and worked-example ownership, preserves supported execution/practice, allows later reassessment without repeating all lessons, distinguishes ready/complete and never saves the hypothetical as evidence.
