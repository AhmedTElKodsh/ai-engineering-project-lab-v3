# Acceptance and preservation

| Capability | Given / when / then evidence |
|---|---|
| CAP-1 | Given initialized native state, when a fresh tutor resumes, then it identifies Q0 / 0.3 and reconciles the unverified app location without pretending a run occurred. |
| CAP-2 | Given a Learn, Assess, Build together or explicit maintenance request, when routed, then the interaction respects that mode and records assistance without imposing a student test on maintenance. |
| CAP-3 | Given the route, when choosing the next increment, then medical extraction and retail retrieval/order operations remain distinct and J0-J5 does not require all advanced branches. |
| CAP-4 | Given execution-only, missing-reference, corrected or newer-failure evidence, when validating promotion, then unsupported completion is rejected; valid later progress remains accepted. |
| CAP-5 | Given the project skill, when metadata and references are validated, then it is discoverable and resolves the native guides; actual tutoring behavior is separately evaluated. |
| CAP-6 | Given the locally reviewed tree, when private publication completes, then visibility and local/remote commit match and frozen sources remain unchanged. |

Source preservation follows `docs/MIGRATION.md` for all seven original guides. The native route preserves J0-J5 and later Q5-Q12, current Q0 / 0.3 retains pending execution/explanation, the skill ledger begins unawarded, and rich first-exposure instruction/inline explanation survives removal of NotebookLM host formatting. Source instruction hierarchy becomes one native hierarchy, not a second mutable ledger.

Run `python tools/validate_workspace.py` and its `--self-test` fixtures. These check structure and selected evidence invariants, not real learning. Use `docs/PILOT.md` for independent tutor simulations and actual learner retention/transfer. Record real results and unavailable checks in the maintainer implementation report. Do not infer any result from the existence of this acceptance document.
