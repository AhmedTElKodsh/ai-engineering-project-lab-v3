# Learner application container

This directory currently reserves the home for the evolving SupportOps application. Only this README exists: no app, installed dependencies, successful provider call or learner achievement is implied.

At J0 inspect the learner's existing app first and continue there when verified. If no app exists and this directory is chosen, guide the smallest first-call program here and record its actual location through the native protocol. Do not scaffold a mature service before the first useful result.

Grow through [release cards](../../docs/project/RELEASES.md). Add package layout, tests, storage and interface only as the active behavior needs them. Run commands/dependency locks belong here after they actually exist. Keep synthetic data, secret values outside tracked content, and private learner evidence out of public history.
