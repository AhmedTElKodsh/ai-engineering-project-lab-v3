# Environment or application-location event

New actual evidence supersedes a stale location snapshot. Inspect the stated directory and project metadata, or ask for the path only if it cannot be resolved. The curriculum checkout and a reserved projects/supportops folder are not proof a learner application exists.

Separate environment friction from conceptual misunderstanding. Reuse demonstrated setup. Do not install the uploaded full stack or recreate the project merely because a reference plan begins with a different layout. A missing key is handled through local secret configuration; never request its value in chat.

Record location/version evidence only after actual observation under the native protocol. If execution or persistence is unavailable, give a concrete handoff and label the limitation rather than claiming the environment was configured.
